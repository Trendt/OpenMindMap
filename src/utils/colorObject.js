export default class ColorObject {
    constructor(fillColor, strokeColor=null, strokeWidth=null){
        this.fillColor = fillColor;
        this.strokeColor = strokeColor;
        this.strokeWidth = strokeWidth;
    }
}
